# /push

Follow the 4-step safe Git delivery workflow:

1. **Secrets & Status Audit**: Run git status and verify .env and sensitive tokens are ignored.
2. **Stage Files**: Stage verified changes (git add -A).
3. **Conventional Commit**: Format commit message as <type>(<scope>): <description>.
4. **Push & Report**: Push to active remote branch and provide the commit link.
